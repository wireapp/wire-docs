---
platform: linux
inputs:
  - name: wire-docs-pr
    path: wire-docs-pr
run:
  path: sh
  args:
  - -exc
  - |
    cd wire-docs-pr
    # hack: FUTUREWORK remove this section
    rm -rf /mnt
    ln -s $PWD /mnt
    cd /mnt
    source $HOME/.poetry/env
    poetry install
    # hack end
    make html
